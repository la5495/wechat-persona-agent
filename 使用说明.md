# 📖 使用说明（用户视角）

> 这份是给主人自己看的。**技术细节都在 `README.md`**，这里只讲"我该怎么用"。

---

## 一、它到底是什么

一个**替你看着微信、用你的说话风格替你回消息**的程序。它：

- ✅ 只回**你指定的人**（白名单），别人一概不理
- ✅ 说话像你（短句、连发、不写标点、用你的口头禅）
- ✅ 记得住事（谁是同事、上次聊了啥、你在学什么）
- ✅ 被问"你是谁" → 说**（主人的名字）**（绝不说小名，也绝不说自己是 AI）
- ❌ **绝不替你答应任何事**（不说"明天一起玩""几点""没问题""我帮你"）
- ❌ 群聊、公众号一律不回

---

## 二、日常怎么用（两个按钮 + 一句话）

### 1️⃣ 开机后：双击桌面 **🟢「启动微信自动回复」**

> 启动后是**只写草稿**模式 —— 它会把想说的话记下来，但**不会发出去**。
> 💡 如果程序**已经在跑**，再点这个按钮不会重复启动，而是**显示当前状态**
> （程序在不在、开关开没开、白名单有谁、有没有待重发的）。

### 2️⃣ 想让它真回话：发一句"开始"

微信里给 **「文件传输助手」**（你自己的文件助手）发：

```
开始
```

它会把开关打开，并回你一句「已恢复自动对话」。

### 3️⃣ 想收工：双击桌面 **🔴「关闭微信自动回复」**

程序进程真的退出。

> 只想**临时闭嘴**（程序继续待着、不花 token）→ 给文件传输助手发 `停止`；
> 想恢复再发 `开始`。这两件事不一样，别搞混：
> **发「停止」= 暂停回话 ｜ 点「关闭」= 程序退出**。

---

## 三、它什么时候会回、什么时候不回

| 情况 | 会回吗 |
|---|---|
| 白名单里的朋友发来消息（5 分钟内） | ✅ 回 |
| 朋友发的是 5 分钟前的旧消息 | ❌ 不回（过时了） |
| **你正在跟这个人聊天**（90 秒内说过话） | ❌ **让路**，不插嘴 |
| 凌晨 01:30 ~ 07:30 | ❌ 不回（你在睡觉） |
| 群聊 / 公众号 / 不在白名单的人 | ❌ 不回 |
| 消息里有验证码、密码、银行卡号 | ❌ 中止，不发 |
| 你正在用电脑打字（键鼠没停） | ⏸️ 等一下再发（不抢你的焦点） |

---

## 四、它回完话，我怎么知道

| 想看什么 | 在哪看 |
|---|---|
| **它到底发了什么** | `audit.jsonl`（每条自动回复都记着） |
| 它写了但没发的草稿 | `drafts.jsonl` |
| 实时日志 | 看 `logs/service.log`，或再点一次「启动微信自动回复」看状态 |
| 现在什么状态 | 再点一次桌面「启动微信自动回复」（在跑就直接显示状态） |

命令行看审计（最近 5 条自动回复）：
```powershell
cd <项目目录>
Get-Content audit.jsonl -Tail 20 -Encoding UTF8
```

---

## 五、常见需求怎么办

### 「我想让 XX 也能自动回」
```powershell
cd <项目目录>
& '.\.venv\Scripts\python.exe' whitelist.py add "张三"
& '.\.venv\Scripts\python.exe' whitelist.py list
```
（名字要跟微信里显示的**会话名**一致）

### 「我不想让它回某个人了」
```powershell
& '.\.venv\Scripts\python.exe' whitelist.py remove "张三"
```

### 「我想先看看它会怎么回，再决定发不发」
**别发「开始」就行** —— 程序在只写草稿模式下，会把想说的都记进 `drafts.jsonl`，
你翻一翻就知道效果。

### 「它刚才有句话我不满意」
直接自己再发一条真的过去盖掉就行（它不会跟你抢话，90 秒内会给你让路）。

### 「我想改它说话的风格」
告诉人家（AI 助手）"太长了/太客气了/别老用某个词"，人家调规则后重跑人设。

### 「微信升级了 / 它突然不工作了」
跑一次更新（人家每次会话开始时也会自动跑），简报里会写清楚：
```powershell
& '.\.venv\Scripts\python.exe' update.py
```
- 看到 `🔴 本该在跑却停了` → 重开一下程序
- 看到 `❌ 兼容` → 微信版本变了，**先别开自动回复**，告诉人家

### 「我就想让它闭嘴一小时」
发 `停止`；想恢复再发 `开始`。程序一直在跑，不花一分钱。

---

## 六、开工前请记住三件事（重要）

1. **它用的是你的身份说话** —— 朋友会当真。所以：
   - 白名单**只加你信得过、也了解情况的人**
   - 重要的、涉及钱和承诺的话，**自己说**
2. **它不会替你答应任何事**（这是硬规则 + 双重过滤），但它可能会**含糊其辞**——
   朋友追问时，可能显得你有点敷衍。这是为了安全，故意这么设计的。
3. **它挂了不会自己爬起来** —— 想让它 24 小时在，得让它一直开着；
   建议每天开机后双击一次启动按钮。

---

## 七、一页速查

```
开机  → 双击「启动微信自动回复」
开工  → 微信给文件传输助手发「开始」
暂停  → 发「停止」
恢复  → 发「开始」
收工  → 双击「关闭微信自动回复」
看状态→ 再点一次「启动...」（在跑就会显示状态）
看它说了啥 → audit.jsonl
更新语料/记忆 → python update.py
```

---

## 八、命令行版（不想用按钮时）

```powershell
cd <项目目录>
$py = '.\.venv\Scripts\python.exe'

& $py service.py start              # 启动（默认只写草稿）
& $py service.py start --keep-switch # 启动并保留当前开关
& $py service.py stop               # 停止
& $py service.py status             # 状态
& $py service.py health             # 健康检查（一行结论）
& $py service.py logs 40            # 看日志
& $py update.py                     # 更新语料+记忆（微信开着才跑）
& $py whitelist.py list             # 看白名单
```
